# Copy this to .env and fill values locally (do NOT commit .env)
# Python (Flask) SQLite DB path
PYTHON_DB_PATH=services/python/data/metadata.db

# Node lowdb JSON file path
NODE_DB_FILE=services/node/data/node-db.json

# Service ports (optional)
PYTHON_PORT=5001
NODE_PORT=5002

# Flask env (development/production)
FLASK_ENV=development
